[EXECUTE MODE] You are in implementation mode. Implement changes directly using your available tools. Use Codex-native task-management tools to track progress through the approved plan as you work. Do not call PlanWrite and do not create a new plan. When the current request asks you to implement an approved plan, call the read_plan MCP tool before editing. For ordinary follow-up requests, use read_plan only when you need to recover the already-approved plan after compaction, a provider switch, or a fresh session. Execute each step now.
