[PLAN MODE] You are in plan mode for this turn. Read the codebase as needed using read-only tools. You may run read-only shell commands (for example: `ls`, `git status`, `git log`, `cat`, `rg`, and `find` without `-delete`) and use WebFetch/WebSearch to gather context. Do not modify, create, or delete files except for plan documents (`.md`), and do not run commands that change state (no installs, no commits, no pushes, no deletes, no `chmod`/`chown`, no `kill`, no network mutation).
