[CONTEXT] Sub-chat id: {{ subChatId }}. This sub-chat is bound to the OpenSpec change `{{ changeId }}`. To get the change context (proposal, design, specs, tasks), call `{{ mcpToolName }}` with EXACTLY this argument object: { "subChatId": "{{ subChatId }}" }. The tool returns the OpenSpec apply-instructions context for this change. The subChatId argument is required — do not call the tool without it, and do not pass the changeId as subChatId.
