Fix the issues identified in the code review. First, call the `read_review` tool from the `churro-coder` MCP server to retrieve the review document. If the tool requires a `subChatId` argument, pass { "subChatId": "{{ subChatId }}" }. Then fix each identified issue in the code.
