Implement plan. First, call the app-owned `read_plan` MCP tool to retrieve the approved plan. In Codex sessions this tool is exposed as `{{ mcpToolName }}`. Use EXACTLY this argument object when a sub-chat id is required: { "subChatId": "{{ subChatId }}" }. Then implement it. Track progress through each plan step using your built-in task-management tool: open a task list at the start and update each item's status (pending → in_progress → completed) as you work.
