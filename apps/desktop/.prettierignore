# Generated app-server protocol bindings (regenerated by `bun run codex:download`).
# Reformatting these would just churn against the next regen and risks breaking
# the schema headers ts-rs emits.
src/shared/codex-app-server-schema/
